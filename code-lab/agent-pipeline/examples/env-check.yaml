name: environment-check
description: 检查环境变量

steps:
  # 获取 HOME 环境变量
  - tool: env.get
    config:
      name: HOME
      default: "/tmp"

  # 获取 PATH 环境变量
  - tool: env.get
    config:
      name: PATH

  # 按行分割 PATH
  - tool: text.split
    config:
      mode: separator
      separator: ":"
