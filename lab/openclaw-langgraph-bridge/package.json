{
  "name": "openclaw-langgraph-bridge",
  "version": "0.1.0",
  "description": "Bridge OpenClaw agent execution to LangGraph.js workflow nodes",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.mjs",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.2.9"
  },
  "peerDependencies": {
    "zod": "^3.23 || ^4"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.7",
    "zod": "^3.25"
  },
  "keywords": [
    "openclaw",
    "langgraph",
    "agent",
    "workflow"
  ],
  "license": "MIT"
}
