
## Autoresearch Evening (19:00-19:30)

**项目:** Agent Memory Service (projects/agent-memory-service)
**方法:** autoresearch — 3 cycles, 30min total

### Cycle 1: compareMemories(id1, id2) ✅ keep
- 内容相似度 (ngram)、共享/唯一标签、层级对比、权重差异、合并建议
- 5 new tests (371→376), ~35 lines src
- 教训: `new MemoryService(tmp)` vs `new MemoryService({ dbPath: tmp })` — 字符串参数不会设 dbPath

### Cycle 2: tagHierarchy(opts) ✅ keep
- 标签共现层级构建，贪心父-子关系，深度计算
- 4 new tests (376→380), ~55 lines src
- 教训: minCoOccurrence=2 时低频共现不建关系；totalTags 是唯一标签数不是输入标签数

### Cycle 3: rebalance(opts) ✅ keep
- 基于年龄衰减+访问频率+层级加权的权重重计算
- 4 new tests (380→384), ~30 lines src
- 公式: decay=exp(-factor*ageDays), newWeight=min(decay + accessBoost + layerBoost, 1.0)

**结果:** 3/3 cycles kept, 13 new tests (371→384), ~120 lines src added, zero rollbacks
**experiments.tsv:** 3 new entries, all status=keep
