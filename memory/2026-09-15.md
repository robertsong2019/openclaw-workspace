
## 03:00 project-testing cron（mce 契约修复循环）

- 幂等三查通过（当日无 project-testing 提交）。基线扫描：mce 46/46（5d stale，选为主目标：最久未循环+monorepo）、prompt-mgr 409/409（4d）、atc 1823/1823（150s jest）。
- **RED-first 真 bug**：mce `_listen_responses` 只捕 JSONDecodeError，server stdout 打印合法 JSON 非 dict 行（调试打印 list/JSON 字符串）→ `.get()` AttributeError 未捕获 → 监听线程静默死亡 → 之后所有请求永久超时。silent-hang 家族第 4 例。修复：isinstance dict 守卫 → continue。commit 90b4d68（monorepo）。
- +8 tests（2 red + 6 特性化/契约：error→[]、Popen 失败、SIGTERM 升级杀伤、with 块含吞 False 的 API 味标注）。46→54，×2 green 8.5s，cov 85.4→93.6%。
- **observable 教训（第 4 次，已升级 TOOLS.md 永久规则）**：(a) 占位符替换生成脚本必须 compile()——首版裸文本替换出 SyntaxError，红归因错到被测对象；(b) 断言前读返回契约——list_* 空成功与超时同为 []，只能靠延迟区分。
- tsv 行已记（90b4d68）。剩余 11 行未覆盖=竞态依赖异常路径，不值 flaky 测试。
