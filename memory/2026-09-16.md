# 2026-09-16

## 3AM project-testing cycle（cron project-testing-morning）

幂等三查通过（tsv 上次 09-15 03:12、无近 10min commit、无并发会话）。

**基线扫描**
- prompt-mgr: 432/432 绿 pytest 1.1s（409→+23，昨晚 22:09 改动自带新测试；独立 .git）
- agent-task-cli: 1879/1879 绿 jest 153s（1823→+56，昨晚 21:28 改动自带）
- context-forge: 1523 pass + 1 fail = **Node runner IPC flake 第 4 例**（deserialize @ processRawBuffer 文件级、pass 波动 1523→1520、单文件直跑绿、3 run 全红）→ 已知不可修，不追
- mission-control: 24/24 绿但 **cov 55%**，main() 66-167 零覆盖 ← 本轮目标（7d stale）

**mission-control 24→33, cov 55%→99%**（commit 9e326e3，own repo）
- main() 大单体不重构，走注入接缝：`bdd.sh`/`bdd.NOW_MS` patch + builtins.open 拦 sessions.json
- 5 集成契约：cronSummary 计数/errorJobs 格式、active24h 边界（<=24h 含）、token 汇总、mem/disk MB 数学、recent cap@20 + git section + 时间戳格式
- TestShReal 4：stdout-only/strip/非零退出静默/cwd——覆盖 11-12（真缺口非伪影，之前误疑）
- data/dashboard-data.json 测试中备份/恢复保护真实产物
- 0 red bug（characterization cycle，main 逻辑直白）

**流程教训**
- `--cov=prompt_manager` 报 module-not-imported → 包名是 `prompt_mgr`，先 `ls` 确认真包名再跑 cov
- 判 runner flake 三件套：错误层（IPC 非断言）+ pass 数波动 + 单文件直跑对照

## 04:00 documentation-morning ✅

- **幂等三查**：最近 doc commit = 昨日 c9b0e22（C574-576 追平）；今日无 doc commit、无并发 doc 会话 → 增量成立（C577/578/579 三 cycle 待追平）
- **数字核实**：套件链 10621→10638(C577 +17)→10657(C578 +19)→**10676**(C579 +19)，从 kd-1/2/3 memory 笔记 + git show --stat 逐一验尸；banked 315→316→319→**324 (0.648)**
- **README**：badge 10621→10676；新增 "Cycles 577-579: 0.630→0.648 — 具名假日、列表回忆、相对时间锚定" 段（C577 holiday-entity / C578 list-recall / C579 reltime-anchor，含判别器、size==n 结构门、frame 门、唯一性门、wire-format/多词捕获课）
- **TUTORIAL-ANSWER-FACES**：§5.28-5.30 三节（锚课/结构课/时间课 blockquote）；速查表 +3 行；**带走原则第 11 条**：demand 词的作用域要显式绑定（cardinal 绑问题端、偏移在问题端解析——混用即自我寄生）；footer 增补链
- **提交**：b483ebe（2 files +40/-2，staged diff 验尸全为己编辑，memory_graph.py 脏 hunk 未碰）→ push 62afcd9..b483ebe

## 5:00 essay-generation-morning（cron f0362050）✅

- **幂等三查**：daily-posts 最新 09-15；今日无 essay 产物/commit（origin 最新 e100bf3 昨 20:03）；无并发会话 → 增量成立
- **选题**：昨晚 trending 的 atlas（agent 版本控制，+1091/day）+ worktrunk 命中自家 08-18 checkout 事故/07ca9c3 staged 卷入事故/monorepo 拓扑漂移的亲历痛点；git/版本控制主题此前未写过，与 09-15「测试说谎」（测试工程）题材错开
- **产物**：`memory/daily-posts/2026-09-16-shared-worktree-agent-version-control.md`（~2500 汉字，3+1 代码示例）；`posts/shared-worktree-agent-version-control-2026-09.html`（UTF-8 clean，111 行）
- **核心论点**：git 三隐含假设（知道拓扑/工作树=会话/commit 记录谁与为何）在多 agent 共享工作树下全失效 → 三防线（拓扑探针/staged diff 验尸/transcript 字节级重放 assert count==1）+ atlas-lite（git trailer 写 Session/Trigger/Tests provenance）
- **提交**：f9ef935（index +17 / post +111，staged 验尸干净无外来 hunk），push 成功；commit 本身用了文中 trailer 模式（dogfooding）
- **线上验证**：post 200 ✓、index 引用 2 处 ✓、关键词命中 ✓
## dashboard-update-morning 06:00（cron f38e0365）

- 构建 dashboard-data.json（build_dashboard_data.py）：cron 11 ok / 3 error / 1 running / 15
  - 错误：ai-neuroscience-research（持续性）、key-development-2、key-development-3（今日新增，后两者 last run 均 error）
- sessions 57 总 / 31 活跃24h，tokens 全量 2.99M / 24h 1.99M
- 提交 ef04ad7 并推送；顺带推送了昨日滞留未推的 9e326e3（test: coverage 55%→99%）
- 注：`openclaw sessions list` 不是合法子命令（报 too many arguments），构建脚本直接读 sessions.json，无需该命令

## 8:00 github-trending-deep-analysis（cron 2c714952）✅

- 幂等三查：今日无 trending 产物（最近 09-15-evening）；无近 10min 相关 commit；无并发会话
- 深析对象：**colibri**（+2173/日榜一，纯 C 磁盘流式 MoE，权重 JIT / 五步 token 路径 / 71.6% 一层可预测率 / 双 SSD 加权镜像 / 语义红线「placement 不改语义」）+ **VoiceStudio**（+2776/日增速第一，本地 ElevenLabs 替代，16 TTS+11 ASR 引擎注册表 + 能力矩阵 + MCP Server + OpenAI 兼容 API）
- open-code-review/Agent-Reach/system_prompts_leaks 昨几日已深析，跳过
- 今日主旋律判断：skill 热让位给本地化基础设施三件套（colibri/VoiceStudio/project-nomad），「hold, not rent」成型
- 飞书文档：https://feishu.cn/docx/S3vsdOgwFogU6yx5TVKcSHVInHf（173 blocks，read 验证通过）
- 可落地：amg 借 colibri「placement 只定速度不定语义」红线 + 借 VoiceStudio「按硬件选配置」推荐表格式

## 20:00 晚间深度研究 cron（context-rot）
- 主题：Context Rot 与有效上下文长度（Chroma/NoLiMa/RULER/FLenQA/Anthropic 等 12 源）
- 笔记：catalyst-research/exploration-notes/2026-09-16-context-rot-effective-context.md
- 博文：posts/context-rot-effective-context-2026-09.html（2889 中文字），commit 2460bab 已推送
- 幂等检查通过后执行；与 09-15 时间知识图谱笔记形成闭环（"记忆即压缩"的机制解释）
- next actions 记在笔记内：长度敏感性配对测试、tool result clearing、rerank 小 k、mini-RULER 实测

## 22:00 tool-development-evening（prompt-mgr F24/F25）

- **幂等三查**：今日无 tool-dev 提交（20:00 深研是博客线）；prompt-mgr 昨晚刚做 F22/F23 但 HEARTBEAT 🔴 前两项均 human-blocked（PyPI/npm 命名），acs/sot/atc 待办是 README 非开发 → 继续昨晚选定的 prompt-mgr F 系列（409→432→今晚 447）
- **Cycle 1 (F24)** `validate_all()` + CLI `doctor`：collection 级 lint sweep（offending-only `{name: warnings}`、name 排序、只读）+ manager forward + CLI doctor（表格/OK 摘要）。RED 9 失败 → **真 gap**：`validate()` 抓 lone `{` 漏 lone `}`（对称补齐 +1 测试）。432→442，×2 绿 0.75s。commit 509c585
- **Cycle 2 (F25)** `snapshot()` + CLI `snapshot`：flush-first 契约（先 save 再 copy，直接 collection.add 的未存改动也进快照）、`snapshots/templates-YYYYMMDD-HHMMSS.json` 同秒 `.N` 防撞、主库不动。442→447，×2 绿 0.77s。commit fed0eb9
- **测试课**：3 个 red 阶段测试作者 bug（add 走 `-c` 选项；单 `}` 归 "Lone" 非 "Unbalanced"；rich 把路径折行→regex 先去 `\n`）；tsv 2 行 + features.md 勾选，push 至 2b30cc6

## 23:00 key-development-1 C580（amg who-companion face）

- **幂等三查**：无 kd in-flight 会话；C579 已于 02:17 落地（00966e8+tsv+memory 三连）；/tmp/c579 权威链在位 → 从 C579 队列 top 继续
- **census-first**：offset 21 题 → who-demand 恰 1（gpt4_d6585ce9 "Who did I go with to the music event last Saturday?"）；71017277（giver "from whom"）全角色验尸 evidence-absent（13 命中全假阳性）→ 不做死代码 frame，永久关闭该队列项
- **证据**：target 04-15 的 s17 "saw them live **with Adam Lambert**（表演者）... **with my parents**（同伴）"——marker 绑亲属词 "with my <relation>"+所有格守卫，表演者结构性排除；日期门杀掉旧寄生 pred 本尊（04-01 Brooklyn "group of friends" 句）
- **RED-round 真收获**：attend 变体 pin 抓到 frame regex 死代码（`attend with` 相邻不合法）→ object-gap 版 + 重 census 仍恰 1/500
- **13 迷你测试** verbatim fixture（s17/s1/s11 字节级）→ 13/13 GREEN；C579 reltime 19/19 不动（独立 face 不改历史）
- **Gate**：suite exit=0（本机全量 suite 日志只有 13 字节 exit 行 = C579 同款形态；exit=5 才是没收集）→ replay PASS 1216s：pred-change 恰 {gpt4_d6585ce9}（Brooklyn 句→'my parents'），drift 1 False→True，**banked 325/500=0.650，十六连 keep**
- **Ops 课**：exec 后台+timeout=100 SIGTERM 杀包装 bash 但 pytest 孤儿存活续跑；pgrep -f 自匹配让 while 轮询死循环；chained-sed 把 --chain 指到 OUT 文件（自比零漂移假 PASS 险情）——diff 审计逮住
- commits：ccf70d5（face+wire+tests +251）+ 508cd2a（tsv）；权威链 /tmp/c580/live500_c580.json；next 队列：order-of-airlines sort renderer / 6ae235be context-cardinal / ollama oracle（human-blocked）
