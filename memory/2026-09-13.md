# 2026-09-13

## 3AM project-testing cycle — agent-context-store velocity reconstruction (C-cycle)

- **Result:** acs 3103→3108 (+5), keep. acs commit 9fb179e (own repo, pushed ee23658..9fb179e). tsv row `20260913_0330+08`.
- **Baseline scan:** acs 3103 green ≈15d stale (the 09-11-mapped dedicated coverage target) → primary. langgraph-bridge 280/280 ≈12d, observability 245/245 6d — both stay candidates for next cycle.
- **RED-FIRST bug:** `quality_velocity_tracker` auto-reconstruction only consulted version snapshots; versions hold **pre-mutation** states, so once a key had any version history the current entry content was invisible → checkpoints at/after the newest version read stale quality. Probe: store improved 1h ago → meeting_rate 0.0 / 'stagnant' at now. `store_health_forecast` inherited (exp-smoothing forecast 0).
- **Fix:** current entry state is a candidate state at cp_ts; `entry.updated_at <= cp_ts` guard subsumes the old created_at-only fallback (updated_at ≥ created_at always). +5 tests; region ≥4-line gap runs 5→0; ×2 green 9.5/9.8s.

## Lessons

1. **`--cov-report=` (empty) writes no reports** — analyzed a stale pre-fix cov.json and nearly concluded the fix was a no-op ("still missing" at shifted line numbers). Regenerate via `coverage json` (fresh `.coverage` was there all along) before reading numbers. Corollary: after any source edit that shifts lines, re-derive gap positions before comparing.
2. 09-11's "acs 68% coverage" was a statements-denominator artifact of that scan; real full-suite figure 93.7%. Measure family again — always re-derive denominators when comparing coverage across cycles.
3. acs repo topology: own .git + origin (github.com/robertsong2019/agent-context-store), push works clean. (Updates the mixed-topology picture in TOOLS.md.)

## Next-cycle candidates

- openclaw-langgraph-bridge (≈12d stale, 280 tests)
- acs remaining gaps: store_health_mutation_impact L22971-22976 (6), momentum-label branches in store_velocity_report / store_rhythm_report

## 4AM documentation-morning — amg C564-569 追平（0.594→0.610）✅

- **幂等三查**：昨日（09-12）04:00 cron 未运行（无 commit/memory 条目），上次文档 09-11 prompt-mgr F84；amg README/TUTORIAL 停在 C561-563（77e5c0f 09-10）→ C564-C569 六个 cycle 零文档覆盖，增量成立；无并发会话
- **README**：badge 10413→10491；line 69 轨迹 0.594（Cycles 548-563）→0.610（Cycles 548-569）；新段「Cycles 564-569: 0.594→0.610 — 晋职扣减、持有期限、页码进度与教育年限链」，六 cycle 各一节（C564 弃权注记被 census 翻案为 rescue / C565 have-had 门入口+finish-sum 0.600 / C566 activity-span 会话日期差 / C567 pages 双面 / C568 month-blind+正则回溯陷阱 / C569 教育年限链+resolved negative existence）
- **TUTORIAL-ANSWER-FACES**：主标题陈旧修正 529-548→529-569（前几轮只改了 §5 标题，主标题漏更——本次补上）；§5 扩至 C549-C569+导语加第五波；新增 §5.15-§5.20；速查表 +7 行（C565 双面各一行）；原则 8→9 条（队列注记 ≠ 判决：C564 弃权→rescue、C565 现成→错面）
- **写前双源核实**：六个 cycle 的 commit 哈希/banked/套件数（10413→10423→10437→10448→10463→10476→10491）从 kd notes + experiments.tsv + git log 三方对齐；机制名（_pp_promotion_subtract/pp_have_had_form/_pp_finish_sum/_pp_activity_sum/page_count_sum/education_span）取自 tsv note 与 kd 工件；UTF-8 字节级验证 0 U+FFFD
- 提交 monorepo（显式路径 3 文件；memory_graph.py e04d222d 脏 hunk 第 30 天不碰）
