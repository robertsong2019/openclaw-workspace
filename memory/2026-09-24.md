
## 03:00 project-testing cron — prompt-mgr recent 负数 gate（cea1389, keep）

- **扫描**：全仓 test 文件 mtime 排序（排除昨晚刚做的 4 项目）→ 最陈旧 prompt-mgr（tests 03-29，~6 个月）、ai-dev-tools（08-24）、prompt-template-manager（08-27）。
- **假红陷阱**：ai-dev-tools 裸 `npx jest` 5 suite 全 parse 失败（ESM 项目必须走 `npm test` 的 NODE_OPTIONS=--experimental-vm-modules）——真基线 79/79 绿。先验 runner 再动手，避免幻影修复。
- **prompt-mgr**：基线 475/475（0.86s），99% cov = mined-out → 转行为层探针。**RED ×1**：`recent(-3)` 用裸 `[:n]` 切片于 newest-first 列表 → 静默返回"除最旧 3 条外全部"，exit 0；CLI `-n -3` 直通打印。负数 gate 家族第 3 例（session-archiver cleanOldArchives(-1)、amk prune -5）。修复：models.recent chokepoint ValueError 报值 + CLI IntRange(min=0)；合法性 pin recent(0)==[] / CLI -n 0 exit 0。防御确认（无 bug）：clone 自克隆、rename_variable old==new、bulk_add_tags([]) no-op。
- **探针伪影教训**：updated_at 是 ISO 字符串非 datetime——合成注入 datetime 对象会让 save 崩溃，伪装成产品 bug。注入前先读字段类型。
- 480/480 ×2 绿；prompt-mgr 独立 .git，cea1389 已 push origin master；tsv 行 337。suite 需 `PYTHONPATH=.`（无 pytest 配置，记录未修，surgical）。

## 04:00 documentation-morning cron — C600-C602 文档追平（banked 0.706→0.712）

- **沿用 09-21/09-22 doc 模式**：amg README badge 11029→**11082** + 新节 `## Cycles 600-602: 0.706→0.712 — 构造声明、全句严格头与品牌双墙`（species_total/faith_days/delivery_services 三面，各一整段，含 C501 删行事故与 replay 假 drift 教训全文）；TUTORIAL-ANSWER-FACES 标题范围 529-599→**529-602**（banked 0.494→**0.712**）+ §5.51-§5.53 + 速查表 3 行 + **原则 20→21 条**（构造区分优于仲裁/角色判别优于词面墙 + replay canonical-cp 纪律）
- 编辑核对：README 新节 4 个小节标题 + 许可节完好；TUTORIAL §6 反面教材节完好；footer 增补链补 Cycles 600-602 行、轨迹明细指向 README 532-602
- 下轮 doc 队列：memory_graph demo-orphan 手术 cycle 后补记；counting 队列无现货候选，新 face census 后再追平

## 05:00 essay-generation-morning cron — 《负数是合法的谎言》(commit 729f109)

- **幂等三查通过**：memory/daily-posts 无 09-24 文件、博客 origin 无 09-24 提交、无并发会话——正常首跑
- **选题**：负数 gate 家族三案复盘（昨夜 21:00 session-archiver `cleanOldArchives(-1)` 删全库 + agent-memory-kit `prune -5` 同路；今晨 03:00 prompt-mgr `recent(-3)` 切片双入口说谎，475/475 绿 + 99% cov 没抓到）。与 09-23 晨 amg C599 主题错开，延续晨间"一手实战复盘"系列
- **产出**：`memory/daily-posts/2026-09-24-negative-number-silent-gate.md`（源稿）+ 博客新页 `posts/negative-number-silent-gate-2026-09.html` + index.html 置顶条目；staged 验尸恰 2 文件（+17/+135），commit 729f109 已 push master
- **线上验证**：index 200 / post 200 / 首页引用 ×2
- 要点沉淀：负数 = 语言双入口语义 vs 函数单入口声明的落差；修复三层（chokepoint 报值 / CLI IntRange / exit-0 合法性 pin 配对）；覆盖率 ≠ 语义入口覆盖

## 08:00 github-trending-deep-analysis cron cycle

- 幂等三查通过：今日 memory 无 prior trending 条目（此前最后条目 05:03 essay）、无并发会话
- 榜单 17 项目；最强信号：google/ax（编排层）与 agent-substrate/substrate（沙箱执行层）同日双上榜——agent infra 复刻云原生分层史；obra/superpowers（290K★ skills 框架）因 Skills 生态多期已覆盖而跳过
- 深度分析：**google/ax**（+1543/日，9K★，Go；K8s-for-agents，四原语 Task/Workspace/Gateway/Model；Redis Streams 替代 etcd 作控制面（数百万短命任务压垮 etcd）；actor checkpoint suspend/resume；2C/2GB 玩不动，仅范式观察）+ **DeusData/codebase-memory-mcp**（44.6K★，纯 C 单静态二进制；162 语言 tree-sitter + Hybrid LSP 13 语言；RAM-first 索引（Linux 内核 28M LOC 3 分钟）；边类型认识论分级 CALLS/CALL_REFERENCE/USAGE 与 amg 证据分级教训同构；SQLite FTS5 + 内置 nomic 向量；token 削减 99.2%；arXiv:2603.27277）
- 产物：飞书文档 UwAvd9yCJohpWgxSaYicjbrpnHc（155 blocks，read 验证非空；首写后发现 cotron/SKills 两处笔误整体重写修正）
- ⚠️ Tavily extract 今日 432 限额，改 curl raw.githubusercontent.com 直抓 README 降级成功——下次 tavily 报限额直接走 curl 备援，勿重试
- 下期候选：dream-num/univer（Office Harness，+1142/日）；PanWatch 过 3K★ 再析（A 股盯盘，与 akshare-finance 互补）

## 19:00 github-creative-evening cron — 晚间创新点分析

- 幂等三查通过：memory 无 09-24 evening 报告（上份 09-23-evening）、近 2h 无 commit、晨间 08:00 deep-analysis 已独立完成（google/ax、codebase-memory-mcp、substrate 已覆盖，晚间全部错开）
- 榜单 daily+weekly 双抓；深度分析 3 项目 + 快扫 5：
  - **vectorize-io/hindsight**（daily #1，26.9K★，+1607/日）：仿生记忆分层（world/experiences/observations/mental models）+ retain/recall/reflect 三原语 + 四路并行检索融合；LongMemEval SOTA 且获 Virginia Tech/华盛顿邮报独立复现；**amg 最直接对标**（同基准），60+ 集成 + coding-agent 一键安装 + 每 bank 内置 MCP
  - **alibaba/open-code-review**（weekly #1，40.5K★，+9833/周）："确定性工程 × Agent 混合"——正确性放代码（文件选择/捆绑/规则匹配/定位反思），判断力放模型；同底模 vs Claude Code precision/F1 更高、token 1/9；AACR-Bench 开源；与 prompt-mgr chokepoint 教训同构
  - **JustVugg/colibri**（37.4K★，+2739/周）：纯 C 跑 744B-2.8T MoE，VRAM/RAM/NVMe 单一层次化，"放置只决定速度永不改语义"；权重 JIT（路由一层前瞻 71.6% 可预测）、dual-SSD 加权镜像、负结果科研文化；GLM-5.3-Flash 在支持列表
  - 快扫：treg（agent 工具界 OpenRouter）、CLI-Anything（软件 agent-native 化+CLI-Hub）、univer（连续在榜，下期转正）、orca（并行 agent 舰队 ADE）、ai-memory（Rust 跨厂商记忆交接）
- 元观察：①记忆层周（hindsight/ai-memory/ECC 三项）②确定性脚手架回归（open-code-review+ax 同周）③CLI 成 agent 通用接口 ④推理经济学层次化
- 产物：memory/github-trending-analysis-2026-09-24-evening.md（6.4KB）
- README 抓取全程 curl raw.githubusercontent.com（Tavily extract 432 限额备援，沿用晨间结论）

## 晚间深度研究 cron（20:00）
- 主题：Test-Time Training 测试时训练（近 15 天未覆盖，Tavily 配额尽切 AnySearch）
- 笔记：catalyst-research/exploration-notes/2026-09-24-test-time-training.md（14 篇语料：TTT-Linear/MLP、TTT-E2E、In-Place TTT、Modular TTT、Titans、ARC TTFT 等）
- 博客：posts/test-time-training-fast-weights-2026-09.html，commit 873826b 已推送，index 卡片已加
- 双触发检查：执行前确认当日无笔记、origin 干净，单次执行

## 21:18 code-lab-evening：双触发 intercept + 三 cycle 三 keep（cqc/act/mcpt）

**双触发拦截（幂等纪律第 5+ 例实战）**：21:18 cron 启动时发现 master 已有 3 个 21:08-21:16 的未 push commit（ai-dev-tools 79→93 / skill-doctor 77→81 / prompt-template-manager 29→34，正是本会话的候选清单）——另一会话先完成了同一批工作但**没写 TSV 没 push**。处置：不重做，只补增量——三套件复验绿（93/81/34）+ 补 3 行 TSV + backfill commit 8212816 + push（4 commit 一并同步）。

**自有三 cycle 全 keep（+23 tests）**：
- **C-B cqc 59→66（5bf9e0a）**：--format xml/jsn 静默回落 console rc0（silent-flag-loss）+ **CI 门放行崩溃的检查**（eslint broken config → status:'failed' 被排除 → --fail-on error exit 0——检查没跑成≠检查通过）；--fail-on bogus 原来在全量扫描后才报错，提前到解析期。遗留：json stdout 被进度日志污染（用 --output）；--min-score 同样无视 failed check。
- **C-C act 39→51（4942607）**：setBudget NaN→null 落盘 / 负数预算 / 阈值无校验（storage chokepoint 三门）+ clearLogs('garbage') 静默 0 删除 + **budget check 硬编码 $0.01/$0.03 vs 价格表（gpt-4 应 $90 报 $40）平行常量背离**——calculateCost 统一收口。
- **D mcpt 37→41（77d7b43）**：validate/generate 目录目标 → 裸 EISDIR 未捕获崩溃（今晚 prompt-template-manager 同款）；isFile 门双入口。顺带审计 generate() TS 模板注入面——validate-first + NAME_RE + tsJson 转义，诚实 pass。

**方法教训**：HEREDOC `<< 'EOF'` 写 markdown 无 backtick 风险（TSV 才用 write+cat）；exec preflight 拒 `cd X && node --test` 链，复杂探针一律写脚本文件跑；node --test 套件 flake 未再现（41 tests 规模小）。

台账：experiments.tsv +4 行（backfill 3 + 自有 3，共 7 含 A 轮）。suite 总量 24295 → 约 24342（+47: 14+4+4+5 继承 + 7+12+4 自有）。
