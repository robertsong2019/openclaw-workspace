
## 03:00 project-testing cron — prompt-mgr recent 负数 gate（cea1389, keep）

- **扫描**：全仓 test 文件 mtime 排序（排除昨晚刚做的 4 项目）→ 最陈旧 prompt-mgr（tests 03-29，~6 个月）、ai-dev-tools（08-24）、prompt-template-manager（08-27）。
- **假红陷阱**：ai-dev-tools 裸 `npx jest` 5 suite 全 parse 失败（ESM 项目必须走 `npm test` 的 NODE_OPTIONS=--experimental-vm-modules）——真基线 79/79 绿。先验 runner 再动手，避免幻影修复。
- **prompt-mgr**：基线 475/475（0.86s），99% cov = mined-out → 转行为层探针。**RED ×1**：`recent(-3)` 用裸 `[:n]` 切片于 newest-first 列表 → 静默返回"除最旧 3 条外全部"，exit 0；CLI `-n -3` 直通打印。负数 gate 家族第 3 例（session-archiver cleanOldArchives(-1)、amk prune -5）。修复：models.recent chokepoint ValueError 报值 + CLI IntRange(min=0)；合法性 pin recent(0)==[] / CLI -n 0 exit 0。防御确认（无 bug）：clone 自克隆、rename_variable old==new、bulk_add_tags([]) no-op。
- **探针伪影教训**：updated_at 是 ISO 字符串非 datetime——合成注入 datetime 对象会让 save 崩溃，伪装成产品 bug。注入前先读字段类型。
- 480/480 ×2 绿；prompt-mgr 独立 .git，cea1389 已 push origin master；tsv 行 337。suite 需 `PYTHONPATH=.`（无 pytest 配置，记录未修，surgical）。

## 04:00 documentation-morning cron — C600-C602 文档追平（banked 0.706→0.712）

- **沿用 09-21/09-22 doc 模式**：amg README badge 11029→**11082** + 新节 `## Cycles 600-602: 0.706→0.712 — 构造声明、全句严格头与品牌双墙`（species_total/faith_days/delivery_services 三面，各一整段，含 C501 删行事故与 replay 假 drift 教训全文）；TUTORIAL-ANSWER-FACES 标题范围 529-599→**529-602**（banked 0.494→**0.712**）+ §5.51-§5.53 + 速查表 3 行 + **原则 20→21 条**（构造区分优于仲裁/角色判别优于词面墙 + replay canonical-cp 纪律）
- 编辑核对：README 新节 4 个小节标题 + 许可节完好；TUTORIAL §6 反面教材节完好；footer 增补链补 Cycles 600-602 行、轨迹明细指向 README 532-602
- 下轮 doc 队列：memory_graph demo-orphan 手术 cycle 后补记；counting 队列无现货候选，新 face census 后再追平
