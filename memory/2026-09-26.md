# 2026-09-26

## 项目测试循环（03:00 cron，autoresearch）

- **prompt-mgr 480→486 (+6)**，commit `b629fb2` 已推送。cli.py 97→99%、manager.py→100%，唯一未覆盖行 = `__main__` 守卫（合理保留）。experiments.tsv 已记。
- 6 个行为钉：edit ValueError 后置退出 1 / delete 仅显示失败退出 0（与 edit/restore 不一致，**故意不改退出码**——行为变更需单独决策）/ restore 缺快照、`../` 穿越名、损坏快照三路径 + 两个无副作用 tripwire（不建 safety snapshot、store 字节不变）/ quarantine OSError 兜底。

### ⚠️ 伪影事故（显示层家族新例，已升级 TOOLS.md 永久规则）

基线扫描用 `python3 /tmp/cron_t/run_pytest.py <abs-proj-dir>` 批量跑 → **prompt-mgr 假报 32 收集错误、agent-context-store 假报 721 失败**。根因：script 模式 `sys.path[0]`=脚本目录（/tmp），os.chdir 不改 sys.path，项目包 import 串扰。从项目目录内 stdin heredoc 重跑：**acs 3173/3173、pmgr 480/480、pweaver 223/223 全绿**。教训：批量测试扫描的结果先做单项目复跑验证再当真；好得可疑/坏得可疑的基线数字先查 harness。

### 基线快照（本轮实测）
- projects/prompt-mgr 486/486 (0.9s)
- lab/agent-context-store 3173/3173 (9.6s)
- code-lab/prompt-weaver 223/223 (0.6s)
- 未测（下轮候选）：agent-task-cli (jest)、context-forge (node --test，防 deserialize flake)、a2a-trust-prototype、agent-observability、openclaw-mcp-server、lab/mcp-client-explorer（无 tests 目录）
