# 2026-09-26

## 晚间 code-lab ×4 cycles（autoresearch，21:00 cron，全 keep）

选点策略：stale-first（chain-of-thought/prompt-router 已离库剔除）。全 4 项目基线先扫描确认全绿再动手。

1. **nano-agent 1156→1162**（b4a262e，monorepo，2.8s 套件）
   - tools.py 类型推断：`Optional[int]` / PEP 604 `int|None` → schema 谎报 "string"
   - 坑×2：① Union 无 `__origin__` 映射落回 string；② PEP 604 UnionType **实例没有 `__origin__` 属性**，必须 `typing.get_origin()`（它对两者都返回可比较的 origin）
   - 测试 helper 初版把工具名当参数名查（KeyError），自纠
2. **mcp-client-explorer 60→65**（4ed1d86，monorepo，10s 套件 E2E 子进程）
   - tools/call + prompts/get `arguments` 非 dict → -32603 内部错误 + 异常消息泄漏；统一 -32602 形状门
   - 既有 pin `test_null_arguments_still_responds` 只断言 error 存在不锁 code → null 归入门内不破坏 pin
3. **jp 49→59**（1492f4f，own-git master，已 push 25a476e..1492f4f）
   - 括号内引号键 `["my-key"]`：lexer STRING token + read_string（`\"` `\\` 转义、未闭合/非法转义 LexerError），parser STRING→FieldNode，query 零改动
4. **agent-observability 262→268**（4c229cb，monorepo，2.9s 套件）
   - costEfficiencyCheck：totalTokens 脏字符串/NaN → NaN 毒化 reduce 且 Math.max/min 链全失效 → score=NaN 静默传播（regression 判定 NaN<x 恒 false = 评分器谎报家族第 N 例）
   - 修复：Number.isFinite 门，非 finite 按 0
   - `npx tsx -e` 的相对 import 解析失败——探针直接写成测试文件更稳

总计 +26 tests，10 个 RED-verified，4/4 keep，0 revert。monorepo push 60c30dd..4c229cb。

## 注意事项

- 工作树 `M ai-iot-orchestrator`（gitlink）是他人/其他会话 in-flight 改动——本晚 3 次提交均逐文件 add + `git diff --cached --stat` 验尸，未卷入
- experiments.tsv 已追加 20260926_2130 行；MEMORY.md 四处计数已更新（nano-agent 表行 + "其他"行内 mcx/jp/obs）

## 22:00 tool-dev — atc R82 keep（list 族上半场）
- **R82 F304-F309 Redis list family first half**：lpush/rpush/llen/lrange/lpop/rpop，代码 commit `e5a1eed`，ledger `622e538`，pushed origin/main
- **2010 → 2052 tests**（+42），187/187 suites exit 0（163s），零回滚
- 设计要点：list=plain JS Array 存 cache entry；`_liveListEntry` 镜像 `_liveSetEntry`（expired purge + WRONGTYPE 双向）；`_assertListValues` 先验后写（拒收 push 不碰 key）；`_popList` 空=删 key、部分 pop 保 TTL；rpop count 返回 [tail,...] 序（Redis RPOP k 2 → ['c','b']）
- RED-first 39 fail → 42/42 green，单发全绿零 GREEN 期 bug（R80/R81 各抓 2 个，本轮全靠前置设计对齐 Redis 语义）
- list 族 0 → 6 法；余 lpushx/rpushx/lindex/lset/linsert/lrem/ltrim/rpoplpush/lmove（R83 候选）

## 22:30 ai-neuroscience-research cron — 关键期（#52 补做交付）

- **幂等三查**：topic 池 #52（09-25 新增）但完成表无 09-25 行、memory 09-25/09-26 无 neuro 条目、reports/ 最后 09-20——判定昨晚 cron 中断未交付，今晚补做 #52「关键期」
- **Tavily 仍 432 配额耗尽**（连续第二晚），按 TOOLS.md 路由表切 AnySearch（academic domain），5 组检索 + 2 篇 arXiv abstract 直抓
- 核心材料：Elgaar 2026（LLM 课程学习相序守恒/倒序失效，arXiv 2601.21698）、Wang 2026（grokking=维度相变 SOC，D 穿越 1，arXiv 2604.04655）、Nardou Nature 2023（致幻剂重开社会奖励关键期）、Dolen & Wilkinson ARN 2026 + NIH PATCH 首个人体试验、Baicalin 2026（重开眼优势可塑性）、Achille ICLR 2019 奠基、Kleinman 深度线性网络、Fukase 2025 系统化识别
- 产物：本地报告 memory/reports/2026-09-26-critical-periods.md + 飞书文档 Ldo7dHXwmoZ3WDxz9XLcrmMvnVd（54 blocks，read 验证非空）+ 消息已发罗嵩（om_x100b644934d528a8c24c0ec4e9208af）
- 台账：完成表补 09-25 中断行 + 09-26 交付行；#52 正式收口

## 23:00 kd-1 C609 — coaster_rides face（keep 0d38475, suite 11224）

- banked **363→364 (0.728)，C565 起 45 连 keep**；replay 首试 14 连；权威链 /tmp/c609/live500_c609.json
- gpt4_e05b82a6「Jul-Oct 乘车计数」GT '10 times'：Mummy three times（10/15）+ Xcelerator bare-rode（10/8）+ Space Mountain three times（9/24，**无 coaster 名词**）+ Mako/Kraken/Manta 枚举（7月，**无 times**）= 10；census 严格头 1/500 零表亲
- 机制三形状优先级：N-times 倍数（word+digit）> 名字枚举（'rode the A, B, and C rollercoasters'）> bare-rode=1；rode 动词 + Jul-Oct 月份墙 + ride-context 墙（coaster 名词或 times，挡 'rode my bike in July'）
- 判分红利：形认领前 judge('10') vs GT '10 times' 走 exact_judge=False；认领 coaster_rides 后 counting_judge 数字优先 _cnt_numval('10 times')==10.0 → '10' 直接 bank
- ⚠️ 新坑：pytest.main() 进程内跑 amg 套件 = **静默 exit-0 空日志**（测试模块顶部 os.execve PYTHONHASHSEED 守卫与 in-process collection 互作用）——shell env 前缀 PYTHONHASHSEED=7 后正常（47 focused / 11224 full ×2 核对 focused 单发即绿）
