# 2026-09-18

## 3AM 项目测试循环 — agent-observability（12d6c30, ea1a4eb）

- **基线**：全场扫描 → agent-observability 最久未直接循环（18d，08-31 rate_limit_tests），245/245 绿 2.7s，cov 99.71 line / 95.54 branch。其他 stale：openclaw-mcp-server 9d、a2a-trust 8d、prompt-mgr 7d（下轮候选）。
- **245→256 (+11)，×2 绿**。4 个 red-verified。
- **Red #1（最重要的发现，silent-data-loss-at-export 家族）**：`exportGenAiOtlp` 把事件 attrs 的 object 值序列化成 `kvlistValue: { values: [] }` **空 kvlist** + 非标准 `__raw` sidecar（grep 全仓库零消费者）。结果：opt-in capture 的 prompt/completion 内容**在导出边界静默丢失**——内存里 mapSpan 是对的，上线就没了。已有测试只断言事件存在，从不查载荷。修复：递归 `toOtlpValue`（array→arrayValue, object→kvlistValue），删 `__raw`。
  - **教训**：导出边界测试必须断言 wire 内容本身，不能只断言「事件存在」。
- **Red #2-3**：memory.write `items` / memory.read `results` 传数组（很自然的调用形状）→ `gen_ai.memory.record.count` 变 `String(array)` 垃圾（"[object Object]"）。spec 说这是 int。修复 `toCount()` 数组塌缩为 .length；lint 规则 5 扩展到 record.count 非整数即违规。
- **特征化（0-red）**：policy-engine `merge()` 完全 0 覆盖的公开 API（additive、category+name 去重不覆写、幂等第二次=0、源不动、本引擎的 disableRule 旗标管住合并进来的规则）；`renameCategory`/`removeCategory` true/false 路径。
- **既有 tsc 错误**：regressions-0907.test.ts:150 在干净 stash HEAD 上同样报 = 环境噪音，非我引入，没动。
- **printf `%s` 漏参数 → tsv 行空列**（09-17 heredoc `$()` 家族第 2 例），Python 修复 + 单独 commit ea1a4eb。下次写 tsv 行直接用 Python f-string 一步到位。
- otel-genai：line 100%，branch 85.07→86.25。

## 下轮候选
- openclaw-mcp-server（9d）、a2a-trust-prototype（8d）、prompt-mgr（7d）、context-forge（8d，慢）。
