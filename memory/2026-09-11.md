# 2026-09-11

## 03:00 项目测试循环（cron，autoresearch 方法论）

**Baseline 扫描**（11 套件）：
- 最久未循环：lab/agent-context-store 13d（2994/2994 green 10s，08-29 后首次）、projects/prompt-mgr 12d（403/403 green 1.1s，99% line cov）
- 近 1-2 天已循环：context-forge（09-10 晨）、mission-control（09-09）、openclaw-mcp-server（09-09 晚）、a2a-trust（09-10 晚 94/94）、mcp-client-explorer（09-10 晚 46/46）
- agent-task-cli / agent-memory-service：09-10 晨基线绿（1800 / 724）

**主目标 prompt-mgr（403→409，red-first 真 bug）**：
- RED：markdown roundtrip 在模板 content 含裸 ``` 行时**静默丢数据**——to_markdown 写死 3-backtick fence，from_markdown 精确匹配 "```" 关栅栏，content 自带的 ``` 行提前闭合导出栅栏，其后到下一个 ``` 之间的行全部蒸发（实测 "naughty fence" 消失，content 变成 before\nafter）。**silent-corruption 家族**：export→import 无任何报错，最坏的一类
- 修复（CommonMark 语义）：to_markdown fence = max(3, content 行首 backtick 最长连击 + 1)；from_markdown 只在 ≥ 开栅长度的 backtick 串上闭合，更短的串算 content。精确 3-fence 的老块字节级行为不变（403 个老测试全绿未动）
- +6 tests `test_markdown_fence_safety.py`（3 个对 pre-fix 代码红验证；含 ```` 长 run roundtrip、>= 闭合语义、未闭合长栅栏、back-compat 特征化）。409/409 ×2 green 0.7s
- commit b01c75f，已 push prompt-mgr own-repo origin
- 教训：自己测试里的 fence 检测器第一版写复杂了（rstrip 反向等价判断是错的），修成「第一个纯 backtick 行 = 开栅，长度须 >3」——assert 逻辑和被测逻辑一样要简单可验

**次要：acs coverage 扫描**
- lab/agent-context-store：68% line（12757 stmts / 4084 missing），28.9k 行单文件巨石，缺口已映射，留给未来专门 cycle（本轮预算内啃不动）

**TSV**：experiments.tsv 追加 20260911_0300 行（monorepo commit）
