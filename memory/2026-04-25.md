
## 20:00 — 深度技术研究: MCP Server v2 实现指南

- **研究主题**: OpenClaw MCP Server MVP 实现（TypeScript SDK v2 + Streamable HTTP）
- **产出**: `catalyst-research/exploration-notes/2026-04-25-mcp-server-v2-implementation.md`
- **核心发现**:
  - SDK v2 使用 `registerTool()` + Zod v4 schema，比 v1 更简洁
  - `createMcpExpressApp(transport)` 一行集成 Express + Streamable HTTP
  - 无状态模式（`sessionIdGenerator: undefined`）是 MVP 最佳起点
  - 新特性 `structuredContent` + `outputSchema` 契合 OpenClaw 结构化工具返回值
- **可运行代码**: 完整 3-tools MVP（read_file, run_command, search_memory）+ package.json + curl 测试
- **关联**: 本周 HEARTBEAT 高优先级，Next Actions 已对齐
