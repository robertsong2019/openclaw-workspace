# 2026-09-14

## 3AM 项目测试循环（cron f7510f1f）

**主目标: openclaw-langgraph-bridge**（8d 最久 stale，280/280 基线绿）

### 基线扫描
- langgraph-bridge 280/280 绿 4.7s（09-06 后未循环）
- pocket-agent 58/58 绿；agent-observability 绿；sotk 上一轮 591 绿
- a2a-minimal 的 package.json 已无 test script（之前是 unittest python？待后续 cycle 查明）
- amg suite 已至 10569 tests（评测循环 C570-C573 今日仍在推进 bank 0.622，与本任务无关）
- 外来未跟踪文件：`lab/openclaw-langgraph-bridge/test/multi-agent.test.mjs`（在 glob 内、能跑、全绿，**不是我的，未提交**）

### 覆盖地图
- openclaw-client.ts：15.2% 行 / 0% 函数 = 最大真实缺口（63 LOC 零测试）
- multi-agent.ts 93%：retry-wrap 路径 + 循环依赖路径未覆盖

### 3 个红先 bug（spawn()）
1. **falsy-zero 家族**：`timeoutSeconds: 0` 被 `options?.timeoutSeconds ?` 三元丢弃 → 显式 0（API minimum:0 合法）被静默换成服务端默认。修复 `!== undefined`
2. **unguarded-parse 家族第 4 例**（兄弟：a2a-minimal 6404558 / mcp-client-explorer C2 / openclaw-mcp-server C2）：200 + 非 JSON body（代理 HTML）→ 裸 `SyntaxError: Unexpected token '<'` 零诊断；且 `resp.json()` 已消费 body，错误消息想带 body 都没法带。修复：先 `resp.text()` 再 `JSON.parse` → 诊断含 status + 120 字符片段
3. **裸 fetch 错误**：连接拒绝 → `TypeError: fetch failed`，baseUrl 丢失，生产 node executor 无法定位哪个 gateway 挂了。修复：wrap fetch → `cannot reach gateway at <baseUrl>`

### 特征化 +11（共 +14）
- 请求形状：默认 mode/runtime、apiKey→Bearer、尾斜杠剥离、options 透传、JSON string 响应原样返回
- executor：systemPrompt 空行拼接 / 无 prompt 直通
- non-ok 错误面（status+body）
- AgentPool.createNode retry-wrap：**两层 retry 配置共存**——外层 multi-agent withRetry 读 `maxAttempts`，内层 create-node 读 `maxRetries`（外层存在时内层形同虚设）
- Orchestrator：循环依赖→全标 failure 不挂起；**依赖失败后下游仍执行**（best-effort 语义，pin 住——当前任务间无数据流所以安全，将来加数据传递时这就是炸弹）

### 验证
- 294/294 ×2 绿（4.2s），client 覆盖 15.2→100 行 / 96.3 分支 / 100 函数
- monorepo 提交 e2f3842（tsv+memory 另行提交）

### 教训
- **test-shape 教训第 3 次重复**：本轮 3 次测试重写——`node()` 返回 `${name}Result` 键不是 `.result`；behavior map 按 description 不是 type 键；`makePool()` 无参时 `Object.keys(undefined)` 崩。09-10 context-forge 同款教训（"先查现有测试形状"）——**写断言前必须先读被测源码的返回形状**，下次直接执行
